fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
python-dotenv==1.0.1
requests==2.32.3
httpx==0.27.2
yfinance==0.2.40

# Using newer versions with pre-built wheels to avoid Rust compilation issues
# These versions have wheels available for all platforms
# yfinance handles Yahoo Finance API requests more reliably than direct API calls